# React Router Commerce Lab — прогресс обучения

## Цель проекта

Изучить современный React Router от начального до продвинутого уровня на практике, создавая интернет-магазин на React, TypeScript, Vite, pnpm и styled-components.

## Формат обучения

Каждый урок проходит по схеме:

1. Теория.
2. Конспект от руки.
3. Практическое задание.
4. Проверка кода.
5. Исправление ошибок.
6. Проверка знаний.
7. ESLint и production build.
8. Коммит и push учебной ветки.
9. Объединение завершённого урока с main.

## Текущее состояние

- Завершено уроков: 10 из 18.
- Общий прогресс: примерно 56%.
- Последний завершённый урок: 10.
- Текущий урок: 11.
- Текущая учебная ветка: `lesson/11-protected-routes`.
- Коммит реализации урока 10: `b0b5ca3`.
- Merge-коммит урока 10: `d5f3b26`.

## Завершённые уроки

### Урок 01 — базовая маршрутизация

Ветка: `lesson/01-basic-routing`
Коммит: `da41ffd`

Изучено и реализовано:

- `BrowserRouter`;
- `Routes` и `Route`;
- статические маршруты;
- `Link` и `NavLink`;
- активный пункт меню;
- общая страница 404;
- навигация без полной перезагрузки;
- страницы магазина и главное меню.

### Урок 02 — динамические маршруты товаров

Ветка: `lesson/02-dynamic-product-routes`
Коммит: `3c81a99`

Изучено и реализовано:

- тип `Product`;
- массив товаров;
- маршрут `/catalog/:productId`;
- динамический сегмент URL;
- `useParams`;
- преобразование и проверка `productId`;
- поиск товара через `find`;
- отдельная обработка отсутствующего товара;
- карточки товаров через `map`;
- динамические ссылки;
- форматирование цены через `Intl.NumberFormat`.

### Урок 03 — layout и вложенные маршруты

Ветка: `lesson/03-layout-and-nested-routes`
Коммит: `57790f7`

Изучено и реализовано:

- `ShopLayout`;
- общий `Header`;
- `Outlet`;
- layout route без `path`;
- вложенные маршруты;
- индексные маршруты;
- route-префикс `catalog`;
- относительные пути дочерних маршрутов;
- относительные ссылки;
- переход к родителю через `to=".."`.

## Завершённый урок 04

### Search params и фильтрация каталога

Ветка: `lesson/04-search-params-and-filters`

Коммит: `07e1979`

Изучено и реализовано:

- структура URL;
- различие route params и search params;
- тип `ProductCategory`;
- техническое поле `categoryId`;
- `useSearchParams`;
- чтение параметров через `searchParams.get`;
- различие `null` и `undefined`;
- проверка параметров через type predicate;
- фильтрация товаров;
- изменение URL через `setSearchParams`;
- сброс search params;
- доступные кнопки с `aria-pressed`;
- сохранение фильтра после обновления страницы;
- поддержка Back и Forward;
- обработка неизвестной категории;
- успешные ESLint и production build;
- проверка знаний.

### Урок 05 — программная навигация и location

Ветка: `lesson/05-navigation-and-location`

Коммит реализации: `a9f68b3`

Изучено и реализовано:

- различие декларативной и программной навигации;
- хук `useNavigate`;
- переход через `navigate`;
- передача объекта через `location.state`;
- тип `CartNavigationState`;
- получение данных через `useLocation`;
- проверка navigation state во время выполнения;
- поиск товара по переданному `productId`;
- обработка прямого входа на страницу корзины;
- возврат по истории через `navigate(-1)`;
- перенаправление через компонент `Navigate`;
- замена записи истории с помощью `replace`;
- редирект с `/shop` на `/catalog`;
- успешные ESLint и production build;
- проверка знаний.

### Урок 06 — переход на Data Router

Ветка: `lesson/06-data-router-migration`

Коммит реализации: `323c8c8`

Merge-коммит: `42f85be`

Изучено и реализовано:

- Declarative Mode и Data Mode;
- `createBrowserRouter` и `RouterProvider`;
- объектная конфигурация маршрутов;
- layout, `children`, index, динамические и wildcard-маршруты;
- сохранение относительных ссылок и `Outlet`;
- успешные ESLint и production build;
- проверка знаний.

### Урок 07 — loaders и данные маршрута

Ветка: `lesson/07-loaders-and-route-data`

Коммит реализации: `212e155`

Merge-коммит: `e1d626b`

Изучено и реализовано:

- `loader` и `LoaderFunctionArgs`;
- получение route params в loader;
- `productLoader`;
- `useLoaderData` и вывод типа через `typeof productLoader`;
- разделение получения данных и отображения;
- успешные ESLint и production build;
- проверка знаний.

### Урок 08 — ошибки маршрутов

Ветка: `lesson/08-route-errors`

Коммит реализации: `c2abfe7`

Merge-коммит: `61ec683`

Изучено и реализовано:

- `errorElement`, `useRouteError` и `isRouteErrorResponse`;
- выбрасывание 404 через `data`;
- обработка неправильного и отсутствующего `productId`;
- всплытие ошибок до ближайшей границы;
- различие wildcard 404 и data 404;
- успешные ESLint и production build;
- проверка знаний.

### Урок 09 — actions и формы

Ветка: `lesson/09-actions-and-forms`

Коммит реализации: `2a37aed`

Merge-коммит: `5045c32`

Изучено и реализовано:

- `action`, `ActionFunctionArgs` и `request.formData`;
- React Router `Form` и `useActionData`;
- сервероподобная валидация формы;
- различие `name` и `id`;
- вывод ошибок и успешного результата;
- успешные ESLint и production build;
- проверка знаний.

### Урок 10 — pending UI и fetcher

Ветка: `lesson/10-pending-ui`

Коммит реализации: `b0b5ca3`

Merge-коммит: `d5f3b26`

Изучено и реализовано:

- `useNavigation`;
- состояния `idle`, `submitting` и `loading`;
- блокировка кнопки и pending UI;
- `useFetcher`, `fetcher.Form`, `fetcher.state` и `fetcher.data`;
- отправка без изменения URL и истории;
- различие глобальной навигации и локального fetcher;
- успешные ESLint и production build;
- проверка знаний.

## Текущий урок

### Урок 11 — авторизация и защищённые маршруты

Ветка: `lesson/11-protected-routes`

Статус: теория ещё не начата.

План:

- [ ] Разобрать аутентификацию и авторизацию.
- [ ] Создать имитацию пользовательской сессии.
- [ ] Создать страницу входа и login action.
- [ ] Создать защищённую страницу профиля.
- [ ] Проверять авторизацию в loader.
- [ ] Выполнять `redirect` на страницу входа.
- [ ] Сохранять адрес назначения.
- [ ] Реализовать выход.
- [ ] Проверить прямой вход и обновление страницы.
- [ ] Запустить ESLint и production build.
- [ ] Пройти проверку знаний.
- [ ] Создать коммит и выполнить push.
- [ ] Объединить урок с `main`.

## Оставшийся план

### Продвинутая архитектура

- Урок 11 — авторизация и защищённые маршруты.
- Урок 12 — lazy loading и разделение бандла.
- Урок 13 — optional segments, splat, `handle`, `useMatches`.
- Урок 14 — блокировка навигации и UX переходов.

### Production

- Урок 15 — тестирование маршрутов.
- Урок 16 — доступность и управление фокусом.
- Урок 17 — checkout, профиль и завершение магазина.
- Урок 18 — GitHub Actions, deployment и финальный аудит.

## Как продолжить после перерыва

1. Открыть репозиторий.
2. Прочитать этот файл.
3. Проверить текущую ветку:

   `git branch --show-current`

4. Получить изменения:

   `git pull`

5. Установить зависимости при необходимости:

   `pnpm install`

6. Запустить проект:

   `pnpm dev`

7. Отправить ассистенту ссылку на репозиторий и текущий вывод:

   `git log -1 --oneline`

## Правило обновления прогресса

После каждого урока необходимо:

- отметить выполненные пункты;
- записать ветку и коммит;
- указать следующий урок;
- проверить ESLint и build;
- сохранить обновление на GitHub.
